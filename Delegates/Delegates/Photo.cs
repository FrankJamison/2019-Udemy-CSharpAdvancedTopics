namespace Delegates
{
    public class Photo
    {
        // Load a photo from a path
        public static Photo Load(string path)
        {
            return new Photo();
        }

        // Save a photo
        public void Save()
        {
            
        }
    }
}